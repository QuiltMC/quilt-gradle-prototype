pluginManagement {
	repositories {
		gradlePluginPortal()
		mavenCentral()

		maven {
			name = 'Quilt'
			url = 'https://maven.quiltmc.org/repository/release'
		}
	}
}

rootProject.name = 'quilt-gradle'

include 'base', 'minecraft'
